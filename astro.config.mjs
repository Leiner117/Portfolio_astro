import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://leiner117.github.io',
    base: 'https://github.com/Leiner117/minimalist-portfolio'
});
